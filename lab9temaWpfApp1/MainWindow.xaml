<Window x:Class="lab9temaWpfApp1.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:lab9temaWpfApp1"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800" Style="{DynamicResource WindowsStyle}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition />
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <ComboBox x:Name="styleBox" Grid.Row="0"/>
        <TextBlock Text="Header of article" Grid.Row="1" Style="{DynamicResource TextBlockCenterStyle}"/>
        <TextBlock Text="Text of article" Grid.Row="2"  Style="{DynamicResource TextBlockJustifyStyle}"/>
        <UniformGrid Grid.Row="3" Columns="2">
            <Button Content="Back" Style="{DynamicResource ButtonStyle}"/>
            <Button Content="Forward" Style="{DynamicResource ButtonStyle}"/>
        </UniformGrid>
        

    </Grid>
</Window>
